repository {
    dataSourceClassName = org.postgresql.ds.PGSimpleDataSource
    dataSource {
        user = test
        password = test
        databaseName = users
        portNumber = 5432
        serverName = localhost
    }
    connectionTimeout = 30000
}
server {
    portNumber = 8080
    hostName = localhost
}
messageBroker {
    username = test
    password = test
    virtualHost = "/"
    portNumber = 5672
    hostName = localhost
}